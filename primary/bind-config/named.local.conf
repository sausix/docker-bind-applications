//
// Do any local configuration here
//

// We are primary DNS for this zone
zone "example.com" {
	type master;
	file "example.com.zone";
	
	// IP addresses of slave servers which are allowed to request this zone
	allow-transfer {
		192.168.51.50;
	};
};

zone "example2.com" {
	type master;
	file "example2.com.zone";
	
	// IP addresses of slave servers which are allowed to request this zone
	allow-transfer {
		192.168.51.50;
	};
};
